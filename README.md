# Expression Evaluator (Java)

![build](https://img.shields.io/badge/build-passing-brightgreen)
![java](https://img.shields.io/badge/java-17-orange)

## 📌 Описание проекта

**Expression Evaluator** — это учебный Java-проект, реализующий разбор и вычисление математических выражений.
Поддерживаются числа, операции, скобки, переменные и функции.
Если выражение корректно — программа вычисляет результат.
Если содержит ошибки — выводится человекопонятное сообщение об ошибке.

Проект оформлен как Maven-проект, снабжён:

- качественным структурированным кодом;
- JavaDoc-документацией;
- модульными тестами (JUnit 5);
- удобной архитектурой, лёгкой для расширения.

---

## 🚀 Возможности

- ✔ Числа с плавающей точкой (`3.14`, `0.5`)
- ✔ Арифметические операции: `+`, `-`, `*`, `/`, `^`
- ✔ Скобки `(...)`
- ✔ Переменные (`x`, `y`, `radius`)  
  → значения автоматически запрашиваются у пользователя один раз
- ✔ Встроенные функции:
  - `sin(x)`
  - `cos(x)`
  - `sqrt(x)`
  - `max(a, b)`
- ✔ Информативные ошибки при некорректных выражениях
- ✔ Чистая архитектура: Lexer → Parser → AST → Evaluator
- ✔ Поддержка расширения (легко добавлять функции)

---

## 🛠 Требования

- **Java 17+**
- **Apache Maven 3.6+**

---

## 📦 Установка и сборка

### 1. Клонирование репозитория

```bash
git clone https://github.com/JayQust/java-vsulab2-parsing-expression---calculating-its-value.git
cd java-vsulab2-parsing-expression---calculating-its-value

2. Сборка
mvn clean package

3. Запуск тестов
mvn test

4. Запуск программы

Через main:
mvn exec:java -Dexec.mainClass="com.example.App"

Или через собранный JAR:
java -jar target/java-vsulab2-parsing-expression---calculating-its-value-1.0-SNAPSHOT.jar

🖥 Пример работы
2*(x+1) + sin(y)

Введите значение переменной x: 3
Введите значение переменной y: 1.57079632679

Результат = 9.0

📚 Поддерживаемые выражения
Примеры:
Выражение	            Результат
3 + 4 * 2 / (1 - 5)^2	3.5
sqrt(2*2 + 3*3)	        sqrt(13)
max(1, 2+3*4)	        14
2*(x+1)	                зависит от x
sin(3.14/2)	            ≈ 1
🔤 Грамматика (упрощённо)
expression  → term (('+' | '-') term)*
term        → power (('*' | '/') power)*
power       → factor ('^' factor)*
factor      → NUMBER
             | IDENTIFIER
             | IDENTIFIER '(' args ')'
             | '(' expression ')'
             | '-' factor
args        → expression (',' expression)*

🧮 Архитектура
Основные компоненты:
    Класс	Назначение
    Lexer	разбиение текста на токены
    Parser	рекурсивный спуск, построение AST
    ExpressionNode	интерфейс узла AST
    NumberNode	число
    VariableNode	переменная
    BinaryOpNode	бинарные операции
    FunctionNode	вызов функции
    Evaluator	выполняет выражение, хранит переменные
    App	CLI интерфейс
🔧 Добавление своих функций
    Например, добавление функции ln(x):
    case "ln" -> Math.log(args.get(0).evaluate(this));
    Добавить в Evaluator.callFunction().

🧪 Тестирование

Модульные тесты находятся в:
src/test/java/com/example/ParserTest.java

Запуск:
mvn test

Включают:
    проверку корректных вычислений;
    проверку ошибочных выражений;
    тесты приоритета операторов;
    тесты с функциями.

🧼 Стиль кода

Проект следует стандартам:
    Java Code Conventions
    Чёткие классы: Lexer, Parser, Evaluator
    Обязательные юнит-тесты при добавлении нового функционала
    JavaDoc на публичные классы и методы

📌 Changelog
v1.0-SNAPSHOT

    Реализован лексер
    Реализован парсер (recursive descent)
    Построение AST
    Вычисление выражений
    Переменные
    Базовые функции
    JavaDoc
    Unit-тесты
    Maven-проект


✨ Автор

Ярослав
GitHub: https://github.com/JayQust